locust~=1.6
# locust 1.6 depends on the Flask 1.x stack, which modern transitive releases
# broke (Jinja2 3.1 removed `escape`, itsdangerous 2.1 removed `json`,
# Werkzeug/MarkupSafe 2.1 dropped symbols it imports). Pin the compatible
# pre-2.1 stack so `make stress-test` runs.
Flask<2.1
Werkzeug<2.1
Jinja2<3.1
itsdangerous<2.1
MarkupSafe<2.1
coverage~=5.5
pytest~=6.2.5
pytest-cov~=2.12.1
mockito~=1.2.2