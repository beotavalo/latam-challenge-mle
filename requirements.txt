fastapi~=0.86.0
pydantic~=1.10.2
uvicorn~=0.15.0
# Pinned to the 3.x line: fastapi 0.86 allows anyio<5, but anyio 4.x ships a
# pytest plugin that requires pytest>=7, which conflicts with the pinned
# pytest 6.2.5 used by the test targets.
anyio~=3.7.0
numpy~=1.22.4
pandas~=1.3.5
scikit-learn~=1.3.0
joblib~=1.3.0
# Per-IP rate limiting for the public API (abuse protection).
slowapi~=0.1.9
