matplotlib~=3.7.2
seaborn~=0.12.2
# Tooling added for the challenge (lint + experiment tracking + candidate model).
# Dev-only: kept out of requirements.txt so the runtime/Cloud Run image stays lean.
ruff>=0.6,<0.8
mlflow~=2.9
xgboost~=2.0
# mlflow model logging imports pkg_resources, which setuptools removed in 81+.
setuptools<81